--liquibase formatted sql

--changeset liquibase-docs:sql-1
CREATE TABLE TRAVEL_ARRANGEMENT (
	ID NUMBER(10,0) GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
	AGENT VARCHAR2(10) NULL,
	BOOKING_ID NUMBER(10,0) NULL,
	CLIENT VARCHAR2(10) NULL,
	FLIGHT_ID NUMBER(10,0) NULL,
	STATUS NUMBER(10,0) NULL
);
