databaseChangeLog:
- changeSet:
#    preConditions:
#      - onFail: MARK_RAN
#      - not:
#          - tableExists:
#             tableName: BOOKING
    id: 1744620611098-1
    author: joca (generated)
    changes:
    - createTable:
        columns:
        - column:
            autoIncrement: true
            constraints:
              nullable: false
              primaryKey: true
              primaryKeyName: SYS_C008838
            defaultOnNull: false
            generationType: ALWAYS
            name: ID
            type: NUMBER(10, 0)
        - column:
            name: CATEGORY
            type: VARCHAR2(10 CHAR)
        - column:
            name: NAME
            type: VARCHAR2(10 CHAR)
        - column:
            name: PRICE_PER_NIGHT
            type: NUMBER(19, 0)
        - column:
            name: ROOM_TYPE
            type: VARCHAR2(10 CHAR)
        - column:
            name: SERVICE_TYPE
            type: VARCHAR2(10 CHAR)
        - column:
            name: STATUS
            type: NUMBER(10, 0)
        tableName: BOOKING
        tablespace: USERS
#  -- changeset SCHEMA:1
#-- preconditions onFail:MARK_RAN
#-- precondition-table-exists table:BOOKING schema:HOTEL
#
#CREATE TABLE BOOKING (
#    ID NUMBER(10, 0) GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
#    CATEGORY VARCHAR2(10 CHAR),
#    NAME VARCHAR2(10 CHAR),
#    PRICE_PER_NIGHT NUMBER(19, 0),
#    ROOM_TYPE VARCHAR2(10 CHAR),
#    SERVICE_TYPE VARCHAR2(10 CHAR),
#    STATUS NUMBER(10, 0)
#);
#
#-- changeset DML:2
#-- preconditions onFail:WARN
#-- precondition-sql-check expectedResult:1
#--   SELECT COUNT(1) FROM USER_TABLES WHERE TABLE_NAME = 'BOOKING'
#
#-- Insert data into the BOOKING table
#INSERT INTO BOOKING (PRICE_PER_NIGHT, CATEGORY, NAME, ROOM_TYPE, SERVICE_TYPE)
#VALUES (1, 'asd', 'asd', 'asd', 'asd')
#INSERT INTO BOOKING (PRICE_PER_NIGHT, CATEGORY, NAME, ROOM_TYPE, SERVICE_TYPE)
#VALUES (1, 'qwe', 'qwe', 'qwe', 'qwe')
#INSERT INTO BOOKING (PRICE_PER_NIGHT, CATEGORY, NAME, ROOM_TYPE, SERVICE_TYPE)
#VALUES (1, 'zxc', 'zxc', 'zxc', 'zxc')
#INSERT INTO BOOKING (PRICE_PER_NIGHT, CATEGORY, NAME, ROOM_TYPE, SERVICE_TYPE)
#VALUES (1, 'cxz', 'cxz', 'cxz', 'cxz')
