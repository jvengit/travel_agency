purge recyclebin

CREATE TABLE HOTEL.HOTEL (
	ID NUMBER(10,0) NULL,
	NAME VARCHAR2(10) NULL,
	CATEGORY VARCHAR2(10) NULL,
	ROOMTYPE VARCHAR2(10) NULL,
	SERVICETYPE VARCHAR2(10) NULL,
	PRICEPERNIGHT LONG NULL,
	CONSTRAINT HOTEL_PK PRIMARY KEY (ID)
)
